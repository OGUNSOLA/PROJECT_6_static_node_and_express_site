extends layout.pug 

block content
nav.nav
    a(href='/')
      span &larr;
      |  Back
h1.error= ` ${err.message}`
h1.error= `Error status code: ${err.status}  -Server Error`
img.error_img(src='/images/server_error.jpg')